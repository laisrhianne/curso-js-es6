function ePaisagem(largura, altura) {
  //   return largura > altura ? true : false;
  return largura > altura;
}

const ePaisagem2 = (largura, altura) => largura > altura;

const imagem = ePaisagem(5, 20);
console.log(imagem);
