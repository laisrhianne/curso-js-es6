var nome = 'Laís';
var nome = 'Rhianne';
// Não gera erro mesmo estando declarando a mesma variável
// duas vezes.

const nome1 = 'Laís'; // string
const nome2 = "Laís"; // string
const nome3 = `Laís`; // string
const num1 = 10; // number
const num2 = 10.52; // number
let nomeAluno; // undefined -> não aponta para nenhum local na memória
const sobrenomeAluno = null; // nulo -> não aponta para nenhum local na memória
const aprovado = true; // boolean = true, false
